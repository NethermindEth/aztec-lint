# aztec-lint

Phase 1 workspace scaffold for the standalone Rust linter.

## CI Commands

- `cargo check --workspace --locked`
- `cargo test --workspace --locked`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`

Or run all checks with `make ci`.
